<?php
/**
 * This config file defines the auth settings for the Soton CampusM API
 *
 * Note: The username and paswword below are required for API access, but they
 * are not a Southampton login.
 */

/**
 * Interval to fetch the timetable for, starting from NOW.
 * Currently 60 days.
 */
$INTERVAL = new DateInterval('P60D');

/**
 * The username to authenticate with the CampusM API
 */
$CAMPUSM_USER = "";

/**
 * The password to authenticate with the CampusM API
 */
$CAMPUSM_PWD = "";
